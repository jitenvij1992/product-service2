spring:
  application:
    name: product-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    preferIpAddress: true

review:
  application:
    name: review-service

feign:
  hystrix:
    enabled: true